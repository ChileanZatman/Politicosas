<%= form_with(model: political_party) do |form| %>
  <% if political_party.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(political_party.errors.count, "error") %> prohibited this political_party from being saved:</h2>

      <ul>
        <% political_party.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :founding %>
    <%= form.date_select :founding %>
  </div>

  <div class="field">
    <%= form.label :political_compass %>
    <%= form.text_field :political_compass %>
  </div>

  <div class="field">
    <%= form.label :coalition %>
    <%= form.text_field :coalition %>
  </div>

  <div class="field">
    <%= form.label :founding_ideology %>
    <%= form.text_field :founding_ideology %>
  </div>

  <div class="field">
    <%= form.label :principles %>
    <%= form.text_field :principles %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
